import type { NextApiRequest, NextApiResponse } from 'next'; import { authUrl } from '@/lib/youtube'; export default function handler(req:NextApiRequest,res:NextApiResponse){ const { clientId } = req.query; res.redirect(authUrl(String(clientId??'default'))); }
